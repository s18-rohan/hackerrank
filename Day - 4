#include <stdio.h>
int main()
{
   int n;
    scanf("%d",&n);
    if (n%2==0 && n>20)
    {
        printf("Not Weird\n");
    }
    else if(n%2==0 && 2<n && n<5)
    {
        printf("Not Weird\n");
    }
    else 
    {
       printf("Weird\n"); 
    }
    
    return 0;
}
